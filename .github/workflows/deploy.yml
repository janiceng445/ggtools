name: Manual Pages Deploy (placeholder)

on:
  workflow_dispatch: {}

jobs:
  manual-deploy-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Notice
        run: |
          echo "This workflow is now manual-only and acts as a placeholder to avoid automatic runs."
          echo "Use the GitHub Pages automatic build (pages-build-deployment) for regular deployments."
